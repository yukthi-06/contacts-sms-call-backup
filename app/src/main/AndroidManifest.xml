<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    
    <!-- For foreground notifications in WorkManager on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BackupManager"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BackupManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.BackupHistoryActivity"
            android:label="Backup History"
            android:theme="@style/Theme.BackupManager.NoActionBar" />

        <activity
            android:name=".activities.SettingsActivity"
            android:label="Settings"
            android:theme="@style/Theme.BackupManager.NoActionBar" />

        <activity
            android:name=".activities.HelpActivity"
            android:label="Help &amp; Support"
            android:theme="@style/Theme.BackupManager.NoActionBar" />

        <activity
            android:name=".activities.AboutActivity"
            android:label="About"
            android:theme="@style/Theme.BackupManager.NoActionBar" />

        <!-- WorkManager Foreground Service configuration for API 34+ -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    </application>
</manifest>
