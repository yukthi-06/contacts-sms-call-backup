# USER REQUEST
remove 'BackupManager' from all paths

---

# Action Taken
- Modified `StorageManager.java` to remove the `"BackupManager"` directory segment from all generated, returned, scanned, and saved backup paths.
- Backups will now be written directly under the category subfolders (e.g. `Contacts`, `SMS`, `CallLog`) directly within the target directory.
