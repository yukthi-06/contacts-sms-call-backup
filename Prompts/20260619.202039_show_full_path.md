# USER REQUEST
Show the full backup path in Settings page and main page

---

# Action Taken
Modified `StorageManager.getBackupFolderDescription()` to display the full decoded SAF Tree URI string instead of only the folder name.
