# USER REQUEST
I meant, I want all export files to have timestamp field as 'yyyy-MM-dd HH:mm:ss'

---

# Action Taken
- Reverted the call log XML export changes in `XmlExportUtil.java` to format timestamps using `yyyy-MM-dd HH:mm:ss` format.
- Registered custom JsonSerializer implementations for `SmsModel` and `CallLogModel` inside `JsonExportUtil.java` so that their date/timestamp properties are serialized as formatted `"yyyy-MM-dd HH:mm:ss"` string values instead of epoch milliseconds.
- Confirmed that CSV exports also correctly output dates in the `"yyyy-MM-dd HH:mm:ss"` format.
