# USER REQUEST
The settings page shows field as 'External SAF Path'.  It should be only 'Path'.  Also remove 'backupExternalUri' from settings.json

---

# Action Taken
- Changed the label for the backup destination path field in `activity_settings.xml` to "Path".
- Cleaned up `BackupHistoryActivity.java` to use `storage.getExternalPath()` instead of `storage.getExternalUri()`, resolving any compilation issues.
- Verified that `StorageManager.java` does not contain `backupExternalUri` in its settings model, ensuring it will be removed from `settings.json` when settings are saved.
- Added missing `import java.io.File;` to `BackupHistoryActivity.java` to fix the compilation error.
