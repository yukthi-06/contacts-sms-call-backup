# USER REQUEST
settings are not getting saved in '/sdcard/Vypeensoft/Contact_SMS_CallLogs/settings/settings.json'

---

# Action Taken
- Added storage permissions (`WRITE_EXTERNAL_STORAGE` and `MANAGE_EXTERNAL_STORAGE`) to `AndroidManifest.xml`.
- Implemented permission request flows in `MainActivity.java` to prompt the user to enable all-files storage manager access on API 30+, providing authorization to write settings JSON.
