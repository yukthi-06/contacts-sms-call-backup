# USER REQUEST
in the JSON backup file of calllogs, the timestamp is 'milliseconds after epoc'.  make it is in xml 

---

# Action Taken
- Modified `XmlExportUtil.java` inside `exportCallLogs()` to format the `<date>` value using `String.valueOf(log.getDate())` instead of the formatted `yyyy-MM-dd HH:mm:ss` string format, outputting the raw epoch milliseconds timestamp in call log XML backups.
