# USER REQUEST
in the main screen, only show the Backup desination path. Remove the 'change folder' and 'Use internal' buttons. Move these buttons to the Settings for the same field.

---

# Action Taken
- Removed folder actions from `activity_main.xml`.
- Registered `openDocumentTreeLauncher` and folder configuration buttons inside `SettingsActivity.java` and `activity_settings.xml`.
- Overrode `onResume()` in `MainActivity.java` to dynamically sync and update the displayed path.
