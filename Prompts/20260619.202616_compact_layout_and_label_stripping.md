# USER REQUEST
in the main page, remove the text 'External SAF Path :''  to make the field thinner.  
Also make the Export Format height less, so that all data on main screen can appear without scrolling

---

# Action Taken
- Updated `MainActivity.java`'s `updateDestinationUI()` method to strip the `External SAF Path: ` and `Internal Storage: ` prefixes from display path text.
- Re-styled `activity_main.xml` to compact paddings, card margins, grid card padding, and button sizes to fit all controls without scrolling.
