# USER REQUEST
On startup take permission for full disk access. It shold not have any issues related to disk permission after that

---

# Action Taken
- Changed visibility of `StorageManager.loadSettings()` to `public`.
- Updated `MainActivity.java`'s `onResume()` life cycle hook to call `storageManager.loadSettings()`. This guarantees that if full-disk storage permissions are granted on first launch, the settings JSON file is immediately read and loaded correctly.
