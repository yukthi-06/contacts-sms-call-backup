# USER REQUEST
'the path saved in settings.json should also be in user understandable path format.. like /sdcard/Vypeensoft/......

---

# Action Taken
Updated `SettingsData` class and settings saver logic in `StorageManager.java` to serialize and save a clean, human-readable path string (`backupExternalPath`) replacing SAF document root prefixes with `/sdcard/` inside `settings.json`.
