# USER REQUEST
instead of 'content://com.cndroid.externalstorage.documents/tree/primary.", replace it with '/sdcard/'

---

# Action Taken
Modified path decoding logic in `StorageManager.java` to format and replace the default SAF primary document root prefix with `/sdcard/`.
