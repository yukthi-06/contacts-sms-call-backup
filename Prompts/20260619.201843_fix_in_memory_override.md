# USER REQUEST
on selecting new folder, it needs to be updated/reflected in the Settings page, Main screen,

---

# Action Taken
Removed the redundant `loadSettings()` call inside `StorageManager.getBackupFolderDescription()` to ensure the newly selected in-memory SAF path does not get overwritten by old disk configuration values before the user clicks save.
