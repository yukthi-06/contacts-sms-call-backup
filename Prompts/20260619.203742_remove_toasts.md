# USER REQUEST
Dont show Toast messages. Show those messages in 'Status Area' field.
Retain the 'Last Backup' information

---

# Action Taken
Modified `MainActivity.java` to replace all `Toast.makeText` messages (permissions status, format warnings, and backup success states) with inline status updates inside the status area TextView (`tvStatusMessage`), retaining the "Last Backup" timestamp unchanged.
